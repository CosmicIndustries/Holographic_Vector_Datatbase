# This file is now in the holographic subdirectory
# Removed sys.path.insert(0, str(Path(__file__).parent)) as it's no longer needed
import unittest

class TestHolographic(unittest.TestCase):
    # Your test methods go here
    pass
